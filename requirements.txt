# ==============================================================================
# DATA DETECTIVE - VALENCIA  ·  Dependencias directas
# ==============================================================================
# Solo las dependencias que el proyecto importa o necesita directamente.
# pip resuelve automaticamente las transitivas (altair, branca, jinja2,
# xyzservices, tornado, etc.).
#
# Instalacion:   pip install -r requirements.txt
#
# Para reproducir el entorno EXACTO (lock con todas las versiones
# transitivas congeladas), usar:   pip install -r requirements-full.txt
# ==============================================================================

# --- Dashboard / UI ---
streamlit==1.56.0
streamlit-folium==0.27.1
streamlit-autorefresh==1.0.1

# --- Datos y computo ---
pandas==3.0.2
numpy==2.4.4
pyarrow==23.0.1          # motor Parquet de pandas (read_parquet)
openpyxl==3.1.5          # motor XLSX de pandas (exportacion a Excel)

# --- Visualizacion ---
plotly==6.7.0
folium==0.20.0

# --- HTTP / scraping / parsing ---
requests==2.33.1
beautifulsoup4==4.14.3
lxml==6.0.3

# --- Calendarios de eventos (icalevents arrastra icalendar) ---
icalevents==0.3.1

# --- Exportacion PDF ---
fpdf2==2.8.7

# --- Configuracion / secretos ---
python-dotenv==1.2.2

# --- Tests (desarrollo) ---
pytest==9.0.3
